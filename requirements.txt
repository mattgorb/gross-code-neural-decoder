# PyTorch built for CUDA 12.4 — compatible with NVIDIA drivers supporting CUDA >= 12.4
# (e.g. driver CUDA 12.8). This avoids the default PyPI wheel, which targets a newer CUDA
# than some drivers support and silently falls back to CPU.
# On macOS / CPU-only machines this index has no matching wheel, so pip uses the default
# build automatically — the same file works everywhere.
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0
numpy>=1.24.0
stim>=1.13.0
